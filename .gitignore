*.gem
*.rbc
/.config
/.venv/
/coverage/
/rspec_tracer_cache/
/rspec_tracer_coverage/
/rspec_tracer_report/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/

# Used by dotenv library to load environment variables.
# .env

# Ignore Byebug command history file.
.byebug_history

## Specific to RubyMotion:
.dat*
.repl_history
build/
*.bridgesupport
build-iPhoneOS/
build-iPhoneSimulator/

## Specific to RubyMotion (use of CocoaPods):
#
# We recommend against adding the Pods directory to your .gitignore. However
# you should judge for yourself, the pros and cons are mentioned at:
# https://guides.cocoapods.org/using/using-cocoapods.html#should-i-check-the-pods-directory-into-source-control
#
# vendor/Pods/

## Documentation cache and generated files:
/.yardoc/
/_yardoc/
/doc/
/rdoc/

## Environment normalization:
/.bundle/
/vendor/bundle
/lib/bundler/man/

# rspec-tracer is a library: Gemfile.lock is not committed so each
# consumer / CI cell / supported Ruby version resolves its own
# compatible dep set (cf. Bundler's own `bundle gem` scaffold).
# Frees us to pin Ruby-version-conditional gems like sqlite3 without
# the committed lock forcing a single resolution across the matrix.
Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Used by RuboCop. Remote config files pulled in from inherit_from directive.
# .rubocop-https?--*

.DS_Store

# Internal 2.0 revamp planning — kept locally per-maintainer, NOT committed.
# See CLAUDE.md / AGENTS.md for why.
/docs/revamp/

# mutant session history — per-run artifact, not source.
/.mutant/

# HTML reporter frontend toolchain - dist/ IS committed (users run
# nothing), but node_modules + Vite's local caches must never be.
/lib/rspec_tracer/reporters/html/node_modules/
/lib/rspec_tracer/reporters/html/.vite/
/lib/rspec_tracer/reporters/html/.vite-cache/
/lib/rspec_tracer/reporters/html/.eslintcache

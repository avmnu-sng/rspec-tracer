<% content_for :title, t('.title') %>

<h1><%= t('.title') %></h1>

<%= link_to t('.new'), new_user_path, class: 'button primary' %>

<section class="users">
  <% if @users.any? %>
    <%= render @users %>
  <% else %>
    <p class="empty"><%= t('.empty') %></p>
  <% end %>
</section>

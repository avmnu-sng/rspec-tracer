<% content_for :title, @category.name %>

<h1><%= @category.name %></h1>

<section class="posts">
  <% if @posts.any? %>
    <%= render @posts %>
  <% else %>
    <p class="empty"><%= t('.empty') %></p>
  <% end %>
</section>

<%= link_to t('.back'), categories_path %>
